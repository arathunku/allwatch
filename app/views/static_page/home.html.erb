<% provide(:title, 'Strona Główna') %>
<% if signed_in? %>
  <%= render 'shared/look_form' %>
  <ol class="looks">
    <%= render @looks %>
  </ol>
<% else %>
  <div class="hero-unit">
    Powiadomienia o nowych aukcjach na allegro.
  </div>
<% end %> 