<% provide(:title, 'Strona Główna') %>
<% if signed_in? %>
  <%= render 'shared/look_form' %>
  Odświeżanie aukcji zależy m.in. od szybkości Allegro i strona może ładować się dłuższą chwilę po kliknięciu przycisku odśwież, proszę poczekać.
  <ul class="looks">
    <%= render @looks %>
  </ul>
<% else %>
  <div class="hero-unit">
    Powiadomienia o nowych aukcjach na allegro.
  </div>
<% end %> 